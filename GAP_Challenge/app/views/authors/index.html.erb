<div class="form" style="position: absolute;  top: 50%; left: 50%; width: 800px; height: 600px; margin-left: -220px; margin-top: -200px; font-size: 24px; color: gray; opacity: 0.8;">
  <p id="notice"><%= notice %></p>

  <h1>Authors</h1>

  <table>
    <thead>
      <tr>
        <th>First name</th>
        <th>Last name</th>
        <th>Birthdate</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @authors.each do |author| %>
        <tr>
          <td><%= author.first_name %></td>
          <td><%= author.last_name %></td>
          <td><%= author.birthdate %></td>
          <td><%= link_to 'See info', author %></td>
          <td><%= link_to 'Edit author', edit_author_path(author) %></td>
          <td><%= link_to 'Delete author', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
  <%= link_to 'Add an author', new_author_path %><br><br><br>
  <%= link_to 'Back to main page', main_pages_path %>
</div>