<%= form_for(publisher_house) do |f| %>
  <% if publisher_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publisher_house.errors.count, "error") %> prohibited this publisher_house from being saved:</h2>

      <ul>
      <% publisher_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :created_at %>
    <%= f.datetime_select :created_at %>
  </div>

  <div class="field">
    <%= f.label :updated_at %>
    <%= f.datetime_select :updated_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
